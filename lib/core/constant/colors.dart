import 'package:flutter/material.dart';

class AppColors {
  // This Color Used in Body textScreen in onBoardingScreen
  static const Color bodyTextColor = Color.fromARGB(255, 44, 44, 44);
  static const Color primaryColor = const Color(0xFF5cb1df);
}
